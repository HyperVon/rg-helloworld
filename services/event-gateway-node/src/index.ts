export const SERVICE_NAME = 'event-gateway';
export const SERVICE_VERSION = '0.0.0-skeleton';

export function banner(): string {
  return `${SERVICE_NAME} ${SERVICE_VERSION} (Milestone 0 skeleton)`;
}
