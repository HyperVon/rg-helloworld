# libraries/

Shared helper libraries used across services.

| Directory | Purpose | Milestone |
|---|---|---|
| `event-testkit/` | Kafka event helpers for contract and integration tests | 1+ |
| `test-fixtures/` | Golden artifacts and fixture builders | 5+ |
| `generated-contracts/` | Generated clients, server interfaces, validation models | 1 |

Nothing is generated until Milestone 1 (`make contracts`).
